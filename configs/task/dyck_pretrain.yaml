defaults:
  - schema
  - _self_

name: dyck_pretrain
data_path: ./data/paren/nt_dataset
model_save_path: ./models/pythia/paren-model
tokenizer_path: ./tokenizers/paren_tokenizer
num_train_epochs: 1
train_truncation: null  # set at runtime via --pretrain-tokens
run_cn: false
run_blimp: false
is_pretrain: true
